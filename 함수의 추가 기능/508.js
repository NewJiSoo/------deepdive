const multi = (x, y) => x * y;
multi(2, 3); // 6

const arrow = () => { const x = 1; }

const create = (id, content) => ({ id, content })

const sum = (a, b) => {
    const result = a + b;
    return result;
};